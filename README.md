# tickets_button_checker
Application checks if 'buy ticket' button is present on the pages 
specified in ```settings.py``` and sends sms if there is one.

Phone number is also specified in ```settings.py```
